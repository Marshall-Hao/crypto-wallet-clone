<ul>
  <% @wallets.each do |wallet| %>
    <li><%= wallet.name %></li>
    <li><%= link_to 'show', wallet_path(wallet)  %></li>
  <% end %>
</ul>

<%= link_to 'Make a new wallet', new_wallet_path, class:"btn btn-flat"  %>
